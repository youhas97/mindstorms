Q: How random is random.randint()?

A: Pseudo-random.Det är inte så random eftersom det är baserat på en algoritm då
datorn inte kan skapa randomness. Om algoritmen och indatan på algoritmen är
känd så kan man få ut värdet på random.randint().